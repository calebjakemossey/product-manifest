# v2 Hardware Variant Configuration

Configuration for robots with depth camera and RGB camera.

This directory contains variant-specific:
- Model weights and parameters
- Sensor configuration
- Launch profiles

In production, this would include depth camera perception model
weights, sensor calibration data, and launch files for v2-relevant nodes.
